{
  "question": "How much memory is used to store a char in Java, and which flag enables compact string storage?",
  "explanation": "A char in Java uses 2 bytes (16 bits). The COMPACT_STRINGS flag allows the JVM to store strings in a compact 1-byte format when possible, improving memory efficiency.",
  "choices": [
    { "label": "1 byte, LATIN1_MODE" },
    { "label": "2 bytes, COMPACT_STRINGS", "answer": true },
    { "label": "4 bytes, ENCODING_FLAG" },
    { "label": "1 byte, UTF_FLAG" }
  ]
}
