{
  "question": "Which method returns an array of all enum constants in Java?",
  "explanation": "`values()` returns all the constants of the enum in an array.",
  "choices": [
    { "label": "all()" },
    { "label": "enumValues()" },
    { "label": "values()", "answer": true },
    { "label": "getAll()" }
  ]
}
