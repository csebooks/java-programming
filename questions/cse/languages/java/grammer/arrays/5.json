{
  "question": "Which method is used to sort an array?",
  "explanation": "`Arrays.sort(array)` is used to sort the array in ascending order.",
  "choices": [
    { "label": "Arrays.order()" },
    { "label": "sort.array()" },
    { "label": "Arrays.sort()", "answer": true },
    { "label": "sort()" }
  ]
}
