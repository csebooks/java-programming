{
  "question": "How can you loop through all elements in an array using an enhanced for-loop?",
  "explanation": "The enhanced for-loop syntax is: `for (type var : array)`.",
  "choices": [
    { "label": "for (int i = 0; i <= arr.length; i++)" },
    { "label": "for (int i : arr)", "answer": true },
    { "label": "foreach (int i in arr)" },
    { "label": "loop (int i in arr)" }
  ]
}
