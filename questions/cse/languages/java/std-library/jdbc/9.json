{
  "question": "Which method is used to execute a batch of statements in JDBC?",
  "explanation": "executeBatch() executes all added SQL commands at once.",
  "choices": [
    { "label": "execute()" },
    { "label": "executeUpdate()" },
    { "label": "addBatch()" },
    { "label": "executeBatch()", "answer": true }
  ]
}
