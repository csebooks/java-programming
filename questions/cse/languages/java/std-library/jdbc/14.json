{
  "question": "What happens if a batch insert violates a constraint?",
  "explanation": "If not inside a transaction, partial inserts may persist; transactions help rollback all.",
  "choices": [
    { "label": "Only the failed row is ignored" },
    { "label": "Entire batch is always retried" },
    { "label": "Only valid rows are saved" },
    { "label": "Only a transaction can ensure rollback of all changes", "answer": true }
  ]
}
