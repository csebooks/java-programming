{
  "question": "Which method is used to rollback a transaction?",
  "explanation": "rollback() undoes all uncommitted changes in a transaction.",
  "choices": [
    { "label": "cancel()" },
    { "label": "rollback()", "answer": true },
    { "label": "abort()" },
    { "label": "reset()" }
  ]
}
